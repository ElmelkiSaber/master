{% extends "widget-admin.html.twig" %}

{% block body %}
            <a class="btn btn-primary btn-fw" href="{{ path('assurance_new') }}">Create a new asurance</a>
              <div class="row">
            <div class="col-lg-12 grid-margin">
              <div class="card">
                <div class="card-body">
                  <h4 class="card-title">Statut socials</h4>
                  <div class="table-responsive">
                    <table class="table table-bordered">
        <thead>
            <tr>
                <th>Id</th>
                <th>Name</th>
                <th>Numpolice</th>
                <th>Description</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for assurance in assurances %}
            <tr>
                <td><a href="{{ path('assurance_show', { 'id': assurance.id }) }}">{{ assurance.id }}</a></td>
                <td>{{ assurance.name }}</td>
                <td>{{ assurance.numPolice }}</td>
                <td>{{ assurance.description }}</td>
                <td>
                    <ul>
                        <li>
                            <a href="{{ path('assurance_show', { 'id': assurance.id }) }}">show</a>
                        </li>
                        <li>
                            <a href="{{ path('assurance_edit', { 'id': assurance.id }) }}">edit</a>
                        </li>
                    </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>
                                  </div>
                </div>
              </div>
            </div>
          </div>

{% endblock %}
