{% extends "widget-admin.html.twig" %}

{% block body %}
    <h1>Statutsocial</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ statutSocial.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ statutSocial.name }}</td>
            </tr>
            <tr>
                <th>Numentreprise</th>
                <td>{{ statutSocial.numEntreprise }}</td>
            </tr>
            <tr>
                <th>Numagreation</th>
                <td>{{ statutSocial.numAgreation }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('statut_social_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('statut_social_edit', { 'id': statutSocial.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
